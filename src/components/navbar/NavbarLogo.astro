---
import {Image} from "astro:assets";

interface Props {
wsize: number;
hsize: number
}

const { wsize,hsize } = Astro.props;

---

<div class="flex flex-col items-center justify-center">

  <Image src="/favicon.svg" width={wsize} height={hsize} alt="Logo"/>
      <p class="font-bold">Astro S.A.<p>
      <p>Industrias Graficas</p>
 </div>

